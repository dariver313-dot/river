services:
  djmima:
    build:
      context: .
    env_file:
      - .env
    init: true
    restart: unless-stopped
    ports:
      - "127.0.0.1:3101:3001"
    volumes:
      - ./data:/app/data
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    stop_grace_period: 30s
